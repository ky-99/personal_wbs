## Requirements

### 機能要件
- 入力要件
  - すでに用意されている表形式のインプットフォームにタスクタイトル、タスク開始日、期限日、重要度を入力する。
  - 入力のインプットフォームはデフォルトで5行分用意されているが、追加したい場合は「+」ボタンをクリックして追加する。
  - + ボタンは一番下の行にホバーしたタイミングで表示され、クリックすると新しい行が追加される。
  - 最終的に入力が終わったタイミングで、入力したデータをサーバーに送信する。

- 出力要件
  - 入力されたデータをサーバーで処理して、タスクの一覧を表示する。
  - タスクの一覧はタイムライン形式で出力される
  - タイムラインには各タスクの開始日、終了日をバーで表示する
  - タイムラインの進行バーの色は重要度によって変更する
  - タイムラインはsvg, html, csvの3種類で出力できるようにする

- 画面要件
  - 画面はWebブラウザで表示される
  - ベースの背景色は薄い肌色とし、その他のベースカラーは濃いめの水色にする
  - フォントはデフォルトではNoto Sans JPを使用する

- アプリケーション要件
  - アプリケーションはNode.jsで作成する
  - アプリケーションはデータベースを持つ必要はない。入力後、出力ボタンを押して、出力されたデータはダウンロードできるようにする。
  - アプリケーションはnpmで管理する。
  - アプリケーションはReactで作成する。
  - アプリケーションはTailwind CSSでスタイリングする。
  - アプリケーションはReact Routerでルーティングする。
  - アプリケーションはReact Hook Formでフォームを管理する。
  - アプリケーションはZodでバリデーションを管理する。
  - アプリケーションはReact Iconsでアイコンを管理する。

- 基本要件
  - アプリケーションは日本語で操作できる。
  - アプリケーションはシンプルさを重視する。
